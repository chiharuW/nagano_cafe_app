<%= render 'public/sessions/list' %>

<main>
<div class="container pt-5 pb-4">
 <div class="row">
  <div class="col-md-4">
   <h2>会員トップ</h2>
  </div>

  <% @items.each.with_index(1) do |item, i| %>
   <%= order.name %>
  <% end %>

  </div>
 </div>
 </main>