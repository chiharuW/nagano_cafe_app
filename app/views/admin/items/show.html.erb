<h2>商品詳細</h2>
<table>
    <tr>
     <th>商品名</th>
     <th>商品説明</th>
     <th>商品価格</th>
     <th>商品ジャンル</th>
    </tr>
    <tr>
     <td><%= @item.name %></td>
     <td><%= @item.introduction %></td>
     <td><%= @item.price %></td>
     <td><%= @item.genre_id %></td>
    </tr>
</table>

<%= link_to "編集", edit_admin_item_path(@item.id) %>

